#include <stdio.h>
/**
 * main - entry point
 * Return: 0
 * @argc: argument count
 * @argv: argument vector
 */

int main(int argc, char *argv)

{
	for (i = 0; i < argc; i++)
	{
		printf("%s\n", argv[0]);
	}
	return (0);

}
