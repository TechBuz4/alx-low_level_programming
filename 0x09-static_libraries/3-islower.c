#include "main.h"
/**
 *_islower - returning 1 if lowercase
 * Return: return 1 if lowercase
 * @c: c ia a character
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
	return (1);

}
else
{
	return (0);
}

}
